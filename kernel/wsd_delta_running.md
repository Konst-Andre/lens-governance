> живе доки: доки записи не змерджено у `Work_Standard.md` (буфер, wsd 1.8) · стеля 2–3 сесії

# wsd — DELTA (running buffer)

**Що це.** Вхідний лоток для нових правил `Work_Standard.md` — те саме, що
`Lens_cookbook_delta_running.md` робить для Cookbook, а `Lens_stagebench_delta_running.md`
для маніфеста стендів. Третій і останній буфер за схемою «один буфер на канон-ціль».

**Заведено:** 30.07.2026 (governance-пас, сесія A). Наповнений з `wsd_TODO_running.md` р.62–74
(секція «Процес-кандидати у wsd», яка лежала в продуктовому плані QR Lens) + знахідки b27.

**Формат запису — обов'язковий:** тригер → дія → чому → анти-приклад → **детектор (К2)**.
Запис без тригера або без детектора **не мерджиться** — доробляється тут.

**Правило.** Буфер — не архів. Лежить довше 2–3 сесій і прополка wsd не наближається —
мерджити як є, не чекати.

---

## ✅ Закрито цією сесією (v2.26) — лишено як слід, видалити при наступному мерджі

- **ROUTING «куди що записувати»** → канонізовано як **wsd 12.11** (таблиця чотирьох ролей + порогові умови входу + детектор).
- **Розмір канон-файлів** → вирішено виселенням історії з wsd (−43% обсягу) + `Lens_INDEX.md`; для Cookbook — сесія B.
- **ПОДВІЙНЕ ПОЯСНЕННЯ як named правило** → **стале**: вже канонізовано як **wsd 13.3** у v2.25 (28.07.2026). Запис у TODO пережив власну канонізацію на два місяці — прямий анти-приклад до wsd 1.8 (буфер без правила смерті).

---

## 🟡 Відкриті кандидати

### Процес-кандидати у wsd:
- **HARNESS-ШАБЛОН (еталон) — канонити у wsd 2.4 / Cookbook.** Еталон тепер **`QR_Lens_brand_harness_v6.html`** (v6, не v5 — TODO-фікс DONE). Спина шаблону: (1) об'єкт суду локнутий першим, названий у хедері; (2) fidelity-драбина 3 погляди: Loupe (магніфіковано — SEE+тюн) → real-size 1:1 in-context (confirm читабельності) → full-context Phone (пропорція); (3) реальні розміри девайсів (XS+15Pro), реальні токени, обидві теми; (4) повнота важелів (live-контрол, per-theme split, умовні суб-важелі, 🔒 per-важіль); (5) preset-банк + 2 фаворити для compare; (6) складна панель (caret 2.4d, не з'їдає стейдж); (7) Copy-ALL (STATE+CSS); (8) generalization-proof тоглом; (9) input≠rebuild. Урок: 1:1 чесний для CONFIRM, марний щоб ПОБАЧИТИ дрібний елемент → Loupe у тому ж harness. **+ Нове (implPLAN §4):** «**?**»-тогл легенди (згорнутий дефолт) + **карет-згортання доку** (2.4d, thumb-zone) + уроки v1→v3 (Loupe overlap через `scale()`; harness-прес = JS `.pressing` A67 не `:active`; важелі ВНИЗУ). Тригер канону: наступна governance-сесія.
- **Розмір канон-файлів** — wsd ~172KB / Cookbook ~220KB, ростуть щобатчем. Ризик: неосяжність → завантажить контекст. Ціль = вибірковість. Напрямок (на обговорення): розбити обидва на секційні файли + індекс «де що». Дробити НЕ поспіхом (ризик розсинхрону рефів) — дедикована сесія. Пом'якшення діє (гребати цільову секцію).
- **МІКРОСКОП-АУДИТ для складних багатодотичних задач** (кандидат у wsd, прецедент B53). Фікс торкається багатьох станів/шляхів (race, mode-switch, VT, cross-thread) → повільний детальний grep-прохід. Прецеденти: B53 banner-interrupt (`renderBanner` innerHTML-rebuild р.998), 14.16 pointercancel-скрол, 14.20 frame-gap. Тригер = «фікс торкається >1 стану/шляху» → обов'язковий 2-й grep-прохід перед кодом.
  - **+ РОЗШИРЕННЯ (запит Konst, B59 perf-арк): мікроскоп + web-пас = СПАРЕНА discovery-фаза перед lock.** Після self-audit підводних каменів (grep ВЛАСНИХ тверджень, не лише чужого коду) → **одразу веб-пас за best-practice класу проблеми** → ширший набір важелів, які потім комбінуємо/переймаємо/адаптуємо. Розподіл ролей: **web = гіпотези+важелі, device = вирок** (14.18 — research не скасовує device-арбітраж). Чому спарено: мікроскоп ловить чому *наша* ставка ламається; web дає *альтернативні* канонічні рішення, яких могли не знати. Прецедент B59 Tab-2 perf: мікроскоп зловив що `content-visibility` обріже B45 shelf-peek+halo (contain:paint clips ink-overflow — MDN/CSS-Wizardry/W3C підтвердили); web-пас дав канонічний pseudo-opacity glow (Tobias Ahlin/SitePoint/StudioLimb — «filter:drop-shadow не на анімованих») + триангулював корінь (cr0x «avoid huge promoted surfaces / huge layers»). Обидва самі по собі дали б неповну картину. Тригер = мікроскоп-аудит запущено → веб-пас у тій же фазі (перед вибором important/складного фіксу).
- **СКРИПТ-TOOLING (запит Konst, B56.1)** — винести повторювані вручну кроки у проєктні скрипти. Пряма економія токенів мала; головна вигода = **error-prevention** (пропущений гейт = device-фейл = ре-порт цілого батча = дорого) + консистентність між QR/KPI/Drive. Кандидати:
  - **`Lens_validate.py`** — усі гейти §8 в один виклик: `node --check` (усі `<script>` без src) + tag-balance (div/svg/span/button/a/style/script) + A69 dark-твін-паритет (кожне `[data-theme=dark]` правило має `@media prefers-color-scheme` близнюк) + scoped-descendant хінт. Замість 3 ручних bash-блоків щопатч.
  - **`Lens_anchors.py`** — дамп код-якорів батча (номери рядків ключових селекторів/функцій) → менше повторних грепів на claim.
  - **`Lens_scope_diff.py`** — diff база→патч (верифікує що змінені рядки = тільки задумані; ловить випадковий scope-витік).
  Найцінніший — валідатор. Тригер = наступна governance/канон-сесія (НЕ мішати з build-арком).
- **ROUTING-правило «куди що записувати»** (запит Konst, B53). **Пам'ять** = легкий cross-chat вказівник, НІКОЛИ не єдиний дім, НЕ для локнутих/канон (Memory≠канон, 2.1); **plan-TODO** (цей файл) = продуктові кроки + pending-canon ідеї; **wsd** = процес/протокол; **Cookbook** = технічні UI/iOS/motion-патерни. Ціль: Claude сам маршрутизує ідею у правильний дім.
- **ANTI-ПАТЕРН «декоративний left-accent-rail» (запит Konst, Фармастор Home harness 13.07)** — лівий accent-рейл (3px кромка) як дефолтний спосіб «виділити / показати стан» = **вайб-код tell**: спільнота вже впізнає його як маркер згенерованого проєкту. Правило: рейл **лише коли семантично навантажений** (кодує реальну вісь/стан, який більше нічим не читається), НЕ як прикраса чи дефолтна емфаза. Ширше правило-смаку: перш ніж тягтись до звичного акцентного прийому — спитати «це несе зміст саме тут, чи патерн-дефолт?». Замість рейла шукати сигнатуру **з предмету** (напр. лічильний інструмент → число-герой + тон стану). Тригер = будь-яке рішення «додати акцентну лінію/рейл для емфази». *(Claude помічений за дефолтуванням на рейл — свідомо гальмувати.)*
- **ПОДВІЙНЕ ПОЯСНЕННЯ як named комунікаційне правило (запит Konst, Фармастор arc-порт 15.07)** — при НОВОМУ/неочевидному тех.рішенні чи задачі, АБО коли Konst каже «поясни просто»/«не очікував» → давати ОБИДВІ версії поруч: **технічна** (тримає точність) + **проста людська** (заземлює поточний стан), НЕ або/або. Прецедент: arc-fill порт — перше пояснення суто технічне лишило в Konst питання по стану; друге людське зняло → Konst: «мені це сподобалось, робити так надалі». Тригер = виринає нове/неочікуване рішення АБО явний запит «поясни просто». **Формулювання ще уточнити з Konst для «сильного патерну»** (його слова — обдумати як краще занести). Дубль у memory #21 (Memory≠канон → справжній дім тут/у wsd). Канонити у wsd Кластер 1.x (комунікація) в governance-чаті.

---

## Д-1 · Детектор мусить бути правильним (доповнення до К2)

**Джерело:** `StockCheck_session_summary_b27.md` §10.
**Куди:** wsd, секція «Форма запису правила — К1/К2» — **вже внесено у v2.26**, лишено тут
як слід провенансу до наступного мерджу.

**Факт.** Сторож `R6` у матриці b27 упав на першому прогоні, бо регекс `.cta-sweep\{` ловив
і легітимне `.btn-copy.sweeping .cta-sweep{`. **Хибний ✗ шкідливіший за відсутній детектор:**
він привчає ігнорувати червоне, і далі система ламається так само тихо, як без детектора взагалі.

**Детектор детектора:** новий сторож обов'язково прогнати на **чистій базі** — має дати зелене.
Червоне на базі, де порушення немає = регекс завеликий.

> живе доки: назавжди (носій відкритого по governance, wsd 1.8)
> дім оголошено: `Lens_INDEX.md` §5, підрозділ «Черга ядра» · роль — черга
> не-продуктового (`gov 12.11`)
> **Дім файла: `kernel/`** (переїхав з `products/` 31.08.2026 — тека визначає роль:
> `products/` тримає продуктове, борги ядра живуть в ядрі)

# Lens · governance-черга

> Заведено 30.08.2026. Аналог `<Продукт>_CHERGA.md`, але для боргів **ядра**:
> wsd · Cookbook · маніфести · гейти · індекси. Раніше такі пункти жили в
> `EquipLens_CHERGA.md` і витісняли з неї продуктове — черга продукту має стелю 8 KB.

**Правила ті самі:** рядок = покажчик + вік, повний текст лишається там, де народився.
Читається цілком на старті governance-сесії. Правиться на місці, редакцій не має.
**Стеля 8 KB.**

-----

## Відкрите

| id | предмет | вік | умова закриття |
|---|---|---|---|
| `К4-1` | **Лічильник спрацювань правил** — «не спрацювало за N сесій» по `archive/summaries/**`. Вісь для `К3-1` і `IDX-7`. Текст: самері G-G §2 | 18.09.2026 | лічильник у `Lens_validate.py`, нульові у виводі |
| `К5-1` | **Зовнішнє твердження в мікроскопі** (API, платформа, спека) — джерело з датою або позначка «не перевірено». Текст: самері G-G §2 | 18.09.2026 | правило в wsd, `--inject` дає ✗ |
| `G3-1` | **`G3` вимагає оголошення архівних файлів** — ✗, який не закрити (`Ф-5`); 10 із 96 це фон | 18.09.2026 | `G3` виключає `archive/`; ✗ дієві ⟂ фон |
| `К1-1` | **Правила wsd без тригера.** Без «коли вмикається» правило спрацює не там (режим провалу №1). Замір `48` протух: G-F/G-G дали тригери 18 правилам | 30.08.2026 | детектор тригера в `G16` (друга перевірка) |
| `К2-2` | **Смоук-твердження, що самі рахують очікуване** — пастка 4 до `1.15`, текст у буфері `wsd_delta_running.md`. Норматив `✓525` після посилення детектора недійсний | 02.09.2026 | кожне переписане твердження під `--inject` дає ✗; новий норматив ✓N заміряний і оголошений у `Lens_INDEX §5` |
| `Г-11` | Аудит супутника `EquipLens_S17_STARTPOINTS_and_QUEUE_v7.md` — 105 KB, жодного разу не ревізований. Містить оголошення `З-11` і ще 29 З-номерів | 28.08.2026 | супутник розібраний: живе → в доми, мертве → в archive |
| `IDX-7` | Розпил wsd: **194 KiB при межі 200** (G-G) — **блокувальний**: наступна вставка впреться в G6. Ріже не байти, а жанри (`К3-1`) | 30.08.2026 | wsd під 120 KiB або розкладений за жанрами |
| `IDX-8` | `QR_Lens_session_summary_B61_PRESS_PORT_DONE.md` оголошений у §5 і не існує: білд `B61` живий, самері по ньому не написане | 30.08.2026 | самері написане або оголошення знято |
| `IDX-9` | **`G14` і `G15` оголошені в `Lens_INDEX` як гейти, коду в `Lens_validate.py` немає.** Є `G1`–`G13` і `G16`. Індекс обіцяє машинну перевірку, якої не існує — гірше за відсутність гейта: створює хибне покриття. `G14` = у кожного активного продукту заведена `CHERGA`; `G15` = `§5` не протух (два твердження: таблиця самері + однозначність масок, `gov 12.20`) | 31.08.2026 | обидва гейти написані АБО оголошення в `§5` знято |
| `IDX-10` | **Колізія стелі черги.** `Lens_INDEX §5` каже 8 KB, шапка `EquipLens_CHERGA.md` казала 10 KB. Зведено на 8 явочним порядком 31.08 — потрібен вирок, яке число канонічне, і одна адреса, де воно записане | 31.08.2026 | число назване в одному місці, друге посилається |
| `IDX-12` | **Буфери:** stagebench 0 · wsd 0 (G-C ✅) · cookbook **12** (G-D ✅5) | 13.08.2026 | три буфери порожні |
| `IDX-13` | **`ARCHIVE_INDEX` ⟂ дерево:** §2 «плоско» · лічильники · A–D без `Lens_gov/` · §3 ⟂ CHERGA→GC2a. Текст: самері GC3 | 17.09.2026 | = `git ls-files archive/` |
| `G13-1` | **`G13` сліпий до підсекцій `### N.N` без `§`.** Дубль або дірка в ланцюгу `Lens_stagebench_manifest.md` §8 (8.1–8.18) не ловиться — доведено `--inject` G-B3 17.09.2026 (дубль `## §N` ловить ✗). Причина — регекс `sec_re` у `Lens_validate.py` (вимагає `§`, відсікає `N.N`) | 17.09.2026 | `G13` бачить `N.N`-ланцюги; `--inject` дубля `### 8.x` дає ✗; прогін по живому канону без хибних ✗ (12.12) |
| `G16-1` | **`G16` дає хибний ✓:** детектор `### N.N-б` зараховано батьку (`1.15` п.3) · будь-який підрядок «етектор»/«К2» у тілі (G-E2). Обидва — `--inject`. Слабкі ✓: gov `12.12` («Урок про сам детектор (К2)» зараховано) · `12.17`. Тексти: `…GC2a` §2 · `GE2_…` §4 | 17.09.2026 | ✓ лише за `**Детектор…**` у тілі до `###`; `--inject` обох дає ⚠; без хибних ⚠ (12.12) |
| `Г-13` | **Колізія номерів між самері не ловиться:** `В-15` S21 (лок ≠ заборона) ⟂ `В-15` S22 (→ `1.17-б`); S21-текст більше ніде не живе. Текст: там само §2 | 17.09.2026 | S21-`В-15` має дім (з `Г-11`); вирішено: реєстр В-серії чи детектор дублів |
| `К3-1` | **Досяжність правила:** без точки виклику й детектора — мертвий текст. Аудит G-C2b: сироти wsd `1.7` `1.13` `3.8` · биті адреси `wsd 14.x` ×8, `wsd 6.5/6.6` ×2 · «wsd цілком» у 5 файлах при 182 KB · HISTORY без 2.30–2.34. Форма прополки (G-G): гейт (код) · чек-лист pause point 5–9 · ADR-журнал · архів. Вісь — `К4-1` | 17.09.2026 | гейт рахує сиріт і биті адреси; кожна сирота має виклик, детектор або виселена |
| `IDX-11` | **`kernel/check_cherga.py` живе окремим скриптом.** Детектор прополки черг (загублений id · перетин продуктової та gov-черги · порожній крок · стеля). Перевірений в обидва боки 31.08. Дім коду гейта — `Lens_validate.py` (`12.11`), номер `G17` вільний | 31.08.2026 | влито в `Lens_validate.py` як `G17`, окремий файл видалено |

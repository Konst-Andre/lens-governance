> живе доки: назавжди (вічне, wsd 1.8) — метод роботи, не код продукту

# Lens Stage-Bench — device-simulator harness · MANIFEST (canonical blueprint)

> **Провенанс:** виведено з StockCheck `island-harness v1→v2` device-pick (Konst, 21.07.2026).
> Корінь: Konst ідентифікував, що цей КЛАС стенду — *намальований телефон у реальному масштабі
> девайса + повний інтерактивний інструмент + стилізовані згортувані панелі важелів* — дає стрибок
> якості «наче тестую на реальному девайсі» проти старих «зібраних на колінці» комперів. Device-незалежне
> (process/tooling) → канонізується без device-тесту (wsd 14.19 lesson 3).
>
> **Роль файлу (де-роздута мета Konst):** цей файл = **єдина точка правди для bench/harness-тулінгу**,
> що росте тільки покращенням (improve-only), НЕ регресує. wsd 2.4 і Cookbook **лінкують сюди**, а не
> дублюють → governance-файли лишаються стрункими (аналогія: `canon_delta_A45_material_lever_manifest.md`).
> Нові уроки по стендах додаються **СЮДИ**, не розсипаються по wsd.

---

## Проблема (діагноз кореня)

wsd 2.4 має **словник** (compare/bench/harness) і **ергономіку**, але не називає окремо найвищий рівень
стенду — той, що дає відчуття реального девайс-тесту. Наслідок: без named-еталона Claude будує «компер
на колінці» (голі варіанти на сірому фоні), і рішення про **матеріальність / глибину / скрол / вписаність
у реальний масштаб** ухвалюється в недостовірному контексті. Konst-теза: *фіделіті самого середовища —
це і є цінність*, бо тоді дивишся на свій інструмент, а не на рамку навколо нього.

**Ключова теза:** stage-bench ≠ ще один compare-віджет. Це **девайс-симулятор**, що закриває розрив між
«компер» і «device-тест»: ти взаємодієш із повним інструментом у намальованому телефоні реального масштабу,
наче з живим білдом — тому судиш його **як на девайсі**, ще до девайсу.

---

## §1. Словник — рівні стенду (з арсеналу wsd 2.4 + новий верхній рівень)

| рівень | що це | що СУДИТЬ |
|---|---|---|
| **compare** | парасолька: будь-який scratch для вибору оком | — |
| **bench** | ізольований стенд із повзунками | ЛОКАЄ числа (тон/форма/таймінг) |
| **harness** | елемент у вірному лейауті: реальні токени/сусіди/2 теми | контекст (вага/recess/глибина/вписаність) |
| **stage-bench** ⭐ | **harness усередині девайс-симулятора** (namable верхній рівень) | in-device feel: матеріальність · скрол-поведінка · скло · масштаб · «наче білд» |

**Правило поділу (розвиток 2.4):** число — bench; контекстну вагу — harness; **in-device feel
(матеріал/скрол/скло/масштаб) — stage-bench**. Bench-контекст ≠ harness-контекст ≠ device-контекст;
stage-bench — найближчий проксі до останнього без збірки реального білда.

---

## §2. MANIFEST — анатомія stage-bench (інстанціювати ПЕРШИМ, тоді task-specific поверх)

Обов'язкові інгредієнти, що роблять стенд «девайс-симулятором» (lookup, не re-derivation):

| # | інгредієнт | специфіка |
|---|---|---|
| **0** | **⚠️ ПИТАННЯ ДО ЗБІРКИ (обов'язкове)** | **ПЕРШ НІЖ писати код стенду — спитати користувача: «на чому ти крутитимеш цей стенд?»** (телефон / мак / планшет). Від відповіді залежить, чи це канонічний дуал-фрейм (§2-п.3), чи іменований адаптований варіант (§5-варіанти). Без цього питання стенд будується під уявний екран і половина ергономіки летить. *(Прямий запит Konst, 22.07.2026 — «сподобалось, зроби це нормою».)* |
| 1 | **Девайс-рамка** | намальований телефон: cutout (виріз/Dynamic Island), status-bar `9:41` + індикатори, home-indicator |
| 2 | **РЕАЛЬНИЙ масштаб** | контент розміром як на фізичному телефоні (не зумнутий мокап). Канон-фрейми: **XS 375×812** (SA 44/34) · **15 Pro 393×852** (Island 59/34) |
| 3 | **Дуал-фрейм L/R** | два телефони поруч, **per-frame device+theme незалежні** (напр. XS/light ‖ 15Pro/dark) — 2 теми + 2 девайси за один погляд |
| 4 | **Повний інтерактивний інструмент** | реальна поверхня продукту, з якою **взаємодієш** (скрол/тап), не статик-скрін |
| 5 | **Реальні токени/шрифт/іконки** | не приблизні (wsd 2.4: мокап → хибний вибір) |
| 6 | **Живий контент ПОЗАДУ** | для скла/глибини — реальний скрол-контент під поверхнею (FINDINGS §1: суцільний фон = недостовірно) |
| 7 | **Стилізовані панелі важелів** | докнуті збоку/знизу, **НЕ оверлеєм стейджа** (2.4(a)); **згортувані** caret-тоглом для чистого скріну (2.4(d)) |
| 8 | **Copy-bet 📋** | увесь набір важелів → рядок → clipboard **+ текст-бокс** (webview блокує clipboard) — handoff device-значень назад (2.4(b)) |
| 9 | **Пресети-якорі** | P0 (control) + варіанти як швидкі старт-точки |
| 10 | **Міні-легенда** | що робить кожен повзунок; назви повзунків НЕ перекладати (2.4) |
| 11 | **Frame-gap трійка + slow-mo** | якщо motion/perf: max-gap · dropped(>33ms) · Δ-baseline + ×N сповільнення (wsd 2.4, штатно) |
| 12 | **Zoom** | інспекція деталі без втрати масштаб-контексту |

---

## §3. Тверді правила (щоб якість не регресувала)

1. **Девайс-рамка = РЕАЛЬНІ пропорції** (XS/15Pro канон-числа), не довільний прямокутник. *Фіделіті рамки — це і є цінність (Konst).*
2. **Панелі докнуті, НІКОЛИ не оверлеять стейдж** (2.4(a)); згортувані для скріну (2.4(d)).
3. **Інтерактивний, не статик** — має скролитись/тапатись як білд (інакше це компер, не stage-bench).
4. **Реальні токени, обидві теми, per-frame device+theme незалежні** (не слабко-контрольована 2-га тема — 2.4(c)).
5. **Copy-bet обов'язковий** — без нього немає device→back каналу.
6. **Контент позаду** для будь-якої скло/глибина-поверхні (FINDINGS §1).
7. **Slow-mo BAKE:** значення, натюнені на ×N, у канон пишуться ЕФЕКТИВНІ (`сирий × N`), не сирі (2.4).
8. **Згортається ПАНЕЛЬ, а не стейдж.** Кнопка «сховати» ХОВАЄ ІНСТРУМЕНТАРІЙ і **розгортає стейдж на весь екран** — це і є сенс 2.4(d) (чистий контекст-скрін). Ховати стейдж, лишаючи повзунки — **інверсія правила** й помилка. Обов'язкова плаваюча кнопка повернення панелі. *(Прецедент: `StockCheck_materiality_stagebench_v1` — реалізовано навиворіт, device-зловлено Konst.)*
9. **Повноекранний стейдж — не «вікно на пів екрана».** Підріз висоти фрейму допустимий лише як ДОДАТКОВИЙ режим; дефолт — максимум доступної висоти, інакше судиш інструмент у рамці, а не «як білд» (пряме порушення §-тези про фіделіті).
10. **Пресети = ставки, не канон.** Числа з ІНШОГО продукту/ґрунту вставляти лише як **сірий маркер-референс** біля повзунка (напр. «QR: 14 (інший ґрунт)»), НІКОЛИ як мовчазний дефолт. Кожне число, що піде в канон, мусить мати власний повзунок. *(Konst, 22.07.2026: «A65 приймався в іншому проєкті й, найпевніше, не на склі — його цифри нам не підходять, потрібні важелі».)*
11. **Селектор родини при >2 родинах контролів.** Коли тюняться кілька родин елементів (пігулки / чіпи / поля / кнопки-дії), НЕ виводити 4 набори повзунків одночасно (2.4(a) — «менше параметрів на компер»). Натомість: **таб-селектор родини** → той самий набір важелів перемикається, значення зберігаються per-family, + кнопка «застосувати до всіх».
12. **Числа друкуються в UI стенда, не в консоль.** Device-арбітром є телефон, а на телефоні консолі немає: інструмент заміру, що віддає результат у `console.log`, оператор фізично не може прочитати, і правило інструментального заміру тихо вироджується в «підтвердив оком». Замір показується плашкою/оверлеєм **і** віддається рядком у буфер (📋). *(wsd §1.12 / П-25; реалізація-зразок — `StockCheck_port_b29_5_RULER.html`, сесія F2.7.)*

13. **Стенд несе ЛОК-регістр.** Стенд дозволяє вільно крутити будь-який параметр, і через це **положення повзунка й залочене значення візуально не розрізняються**. Сесія, що портує зі стенда в продукт, бере за канон те, що просто лишилось після експерименту. Тому в стенді живуть **літерали `*_valuesLOCK`** поруч із живими, з міткою розбіжності **біля важеля**, а не окремою вкладкою: під час тюну ніхто не відкриває ані вкладку, ані файл. Три вимоги: **(а)** маркер має **три** стани — `= ЛОК` · `ЛОК N` (друкує **канон**, не живе) · `нема ЛОКу`; двох замало, бо порожнє місце біля важеля читається як «збігається», хоча канону може не бути взагалі. **(б)** **Мертві важелі не потрапляють у рядок 📋** — важіль поза областю дії режиму віддає в самері число, яке нічого не робить, і саме воно стає хибним каноном. **(в)** Нове локнуте рішення пишеться у `valuesLOCK` **і** в регістр стенда **одним ходом**; запис лише в одне з двох вважається незробленим. Сам регістр — теж літерал і теж тухне, тому стенд **друкує покриття** `ЛОК N/M важелів`, а добір ведеться **за ознакою** (важіль має `data-k`), не за списком імен. *(Прецедент: `netmark_stagebench_v3_6` розходився з device-локом ярусу Р-46 у трьох полях — зловлено регістром на першому прогоні. Прецедент рота літерала: О-39, стеля `k_max` 1.20.)*

---

## §4-варіанти. Іменовані варіанти анатомії (дозволені відхилення)

Дуал-фрейм (§2-п.3) — канон **за замовчуванням**. Але відповідь на §2-п.0 може його змінити. Дозволені іменовані варіанти:

| варіант | коли | що змінюється | чим компенсується |
|---|---|---|---|
| **dual-frame** (канон) | мак / планшет / широкий екран | — | — |
| **phone-single-frame** | **користувач крутить стенд із телефона** | ОДИН фрейм замість двох; per-stage перемикачі теми й девайса замість per-frame | **hold-to-compare**: кнопка, що на утриманні показує control-пресет у ТОМУ Ж пікселі, на відпусканні повертає тюн. Точніше за два фрейми для матеріальних порівнянь |

**Що НЕ можна жертвувати в жодному варіанті:** реальний масштаб (§2-п.2), живий контент позаду (п.6), інтерактивність (п.4), copy-bet (п.8), повноекранний стейдж (§3-п.9). Варіант міняє **компоновку**, не фіделіті.

*Прецедент: StockCheck materiality-bench, 22.07.2026 — Konst крутить з телефона; дуал-фрейм там або вбив би масштаб, або витиснув повзунки під стейдж.*

---

### §4-в. `icon-compare` — реальний масштаб + сусіди (28.07.2026, StockCheck b25)

Стенд для вибору іконки застосунку. Дозволене відхилення від §2: **немає девайс-фрейма** —
предмет судиться не всередині екрана, а **на робочому столі**, тобто в чужому середовищі.

Обов'язкові елементи:
1. **Реальний масштаб 60pt** (iOS home screen) + маска-скваркл `border-radius ≈ 22.5%`.
2. **Сірі плитки-сусіди** у сітці 4 колонки. Без них судиться іконка окремо на порожньому
   тлі — а на робочому столі такої ситуації не існує ніколи.
3. **Перемикач підкладки** — світлі / темні / фото-шпалери. Головна пастка тут тон-у-тон.
4. **Лупа** з індикатором масштабу і скидом `1:1` (§7).
5. **Ресурси вшиті base64** — стенд відкривається на телефоні одним файлом, без деплою.

Референс: `StockCheck_icon_compare_v1.html`.
Чому не «кинути обидві на робочий стіл»: щоб побачити `apple-touch-icon` на реальному столі,
треба задеплоїти й перевстановити ярлик — два кола заради вибору з двох.

## §4. Коли stage-bench, а коли bench/compare (драбина вибору)

- **plane compare** — 2 статичні варіанти, «краще» видно оком одразу.
- **bench** — треба залочити ізольоване число (тон/таймінг), контекст не критичний.
- **harness** — контекстна вага/recess/вписаність із реальними сусідами.
- **stage-bench** ⭐ — рішення залежить від **in-device feel**: матеріальність скла, поведінка скролу,
  елевація-в-контексті, вигляд у реальному масштабі, «чи почувається як білд». Дорожче будувати — тож
  для рішень, де device-feel і є питанням (і де не хочеш палити ресурс на реальний білд лише щоб глянути).

---

**Поза драбиною — пісочниця.** Якщо питання звучить не «чи гарно виглядає», а «чи правильно
поводиться на краєвих випадках, яких немає в реальних даних» — потрібен не стенд, а **пісочниця**
(копія живого білда з синтетичними даними). Окремий метод, окремий дім:
**`Lens_sandbox_manifest.md`**.

-----

## §5. Канон-лінія (де-роздута дисципліна)

- **Цей файл = точка правди bench/harness-тулінгу.** wsd 2.4 і Cookbook **посилаються сюди** рядком-лінком,
  НЕ копіюють вміст. Новий урок по стендах → append СЮДИ (improve-only), не в governance.
- **wsd 2.4:** лишити словник/ергономіку як є; додати один рядок «stage-bench (девайс-симулятор) — див.
  `Lens_stagebench_manifest.md`» замість розростання.
- **Прецедент** → наступний вільний wsd 14.x (лише згадка bump, факти тут).

## §6. Референс-імплементація (копі-from еталон)

- **`StockCheck_island_harness_v2.html`** — поточний gold-standard: дуал device-frame (XS/light ‖ 15Pro/dark),
  реальний масштаб, повний інтерактивний інструмент зі скролом, P0–P3 пресети, copy-bet, згортувана панель
  (2.4(d)), реальні токени, живий контент під склом. Брати як шаблон нового stage-bench.
- Похідні майбутні стенди наслідують §2-анатомію; відхилення документувати тут.

---

## §7. Append 26.07.2026 — ЗУМ: специфікація + відкритий борг реалізації

> Провенанс: Konst нагадав про «+/− кнопки» під час §2-п.0 StockCheck glyph-бенча.
> Перевірка шаблону виявила, що п.12 записаний, але НЕ реалізований.

**§2-п.12 «Zoom» був одним словом — цього замало.** Розгортаю в специфікацію:

1. **Авто-фіт ≠ зум.** `StockCheck_materiality_stagebench_v2.html` (р.866) має тільки
   `frame.style.transform = sc<1 ? scale(sc) : none` — це **вписування вниз**, щоб фрейм
   вліз у екран. Ручного керування немає. **Авто-фіт не закриває п.12.**
2. **Зум = ручні +/− на ФРЕЙМІ**, поверх авто-фіту. Крок ~10%, діапазон ~0.6×–2.0×,
   кнопка `1:1` (скид на реальний масштаб).
3. **Реальний масштаб лишається дефолтом.** Зум — інструмент інспекції деталі,
   НЕ стартовий стан. Інакше порушується §2-п.2 (судиш зумнутий мокап).
4. **Індикатор поточного масштабу обов'язковий** (напр. «1:1» / «140%»), інакше легко
   залочити число, дивлячись на збільшений предмет, і не помітити цього.
5. **Зум ⟂ hide-panel** (§3-п.8) — два різні інструменти огляду: hide-panel дає
   ПОВНИЙ КОНТЕКСТ (стейдж на весь екран), зум дає ДЕТАЛЬ. Не підміняти одне одним.

**Термінологія зон (канон, щоб називати однаково):**

| селектор | назва | що це |
|---|---|---|
| `#stageWrap` | **стейдж** | робоча зона, де стоїть намальований телефон |
| `.frame` | **фрейм** (девайс-рамка) | сам намальований телефон: корпус, виріз, home-indicator |
| `.screenbox` | **скрінбокс** | екран усередині рамки |
| `.stgScroll` | **скролер** | контент, що скролиться під поверхнею |

**БОРГ:** зум відсутній в обох референс-шаблонах (§6). Перший стенд, що його реалізує,
стає новим copy-from еталоном → оновити §6.

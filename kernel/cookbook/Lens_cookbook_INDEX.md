> живе доки: назавжди (вічне, wsd 1.8) · **читається першим** серед cookbook-файлів
> KERNEL v2 · 31.07.2026 — спільне ядро сімейства Lens (переносне, `Lens_NEWPROJECT_bootstrap.md`)

# Lens — iOS / PWA Cookbook · ІНДЕКС

> **Що це.** Маршрутизатор Cookbook. Сам Cookbook живе у **5 тематичних томах**; цей файл —
> єдиний вхід у них. Крос-продуктовий каталог перевірених iOS PWA + UI/UX патернів для всіх Lens.
> Це **ЩО ми будуємо** (рецепти-рішення). **ЯК ми працюємо** (протокол, pre/post-patch, UX, sync) → `Work_Standard.md`.
>
> **Як читати.** Ніколи не читати том цілком. Знайти рядок у §2 → взяти номер → §3 дає том → читати **точково**.

-----

## §1 Мапа томів

| том | файл | тригер — коли сюди йти | вага |
|---|---|---|---|
| **1** | `Lens_iOS_cookbook_1_platform.md` | заводжу/лагоджу оболонку: head, manifest, іконки, теми-інфраструктура, висоти, safe-area, середовище | 32.1 KB |
| **2** | `Lens_iOS_cookbook_2_navigation.md` | будую навігацію, bottom sheet, свайп рядка, календар/пікер у шіті | 46.0 KB |
| **3** | `Lens_iOS_cookbook_3_material.md` | не той тон · картка не читається · глибина не та · тема ламає характер | 53.6 KB |
| **4** | `Lens_iOS_cookbook_4_components.md` | треба конкретний контрол: селект, тогл, чіп, плитка, тост, нативне поле | 50.9 KB |
| **5** | `Lens_iOS_cookbook_5_motion.md` | воно рухається не так · жест б'ється зі скролом · тап не відчувається · джанк | 56.1 KB |

**Нумерація наскрізна.** A-номери не змінювались при розпилі (KERNEL v2) — усі старі посилання
виду «Cookbook A45» лишаються дійсними. Діапазони томів **розривні**; адресує цей індекс, не діапазон.
**Код A79 живе окремо:** `Lens_module_maint_v1.md` — донор-модуль (поріг wsd 12.11:
споживачів ≥2, коду >100 рядків). Cookbook лишає *чому*, модуль дає *робочий фрагмент*.

**A74 — зарезервований порожній номер** (незаписаний патерн dpicker, `StockCheck_session_summary_dpicker_LOCK.md` §P1).

-----

## §2 Задача → запис → том

| Роблю… | Дивлюсь | Том |
|---|---|---|
| Новий HTML-кістяк продукту (head/manifest/icons) | `A1–A4` | **1** |
| Тема (light/dark/auto), FOUC | `A5–A8` | **1** |
| Підсвітка обраного = глянцева капсула «D2» (toggle/select) | `A6 §3` | **4** |
| Повноекранний layout, висоти, viewport | `A9–A13` | **1** |
| Каркас екрана (top stack, nav, app-bar) | `A14–A16` | **1** |
| Bottom sheet (структура / анімація / свайп) | `A17–A21` | **2** |
| Свайп рядка списку з діями (edit/delete) | `A22` | **2** |
| Блокування (orientation / zoom / long-press) | `A23–A25` | **1** |
| Доступність, motion, sync теми між вікнами | `A26–A28` | **4** |
| Іконки, поліш, z-index, empty state | `A29–A32` | **4** |
| Тип картки: accent-rail + кольорова семантика | `A34–A35` | **3** |
| Chrome форма + dark-elevation (pinned-зона) | `A61` | **2** |
| Range-календар / date-range scope у sheet | `A33` | **2** |
| Ховаю елемент `[hidden]` поряд з `display:flex` | `A36` | **4** |
| Два хрести в полі пошуку (нативний + власний) | `B3.1` | **4** |
| Native-контроли (date/time/select) в один рядок | `A40` | **4** |
| Native picker сірий/не міняє тему в PWA | `A41` | **4** |
| Текст поверх ілюстрації/SVG → скрим | `A42` | **3** |
| Inline layered SVG як фон картки/геро | `A43` | **3** |
| Об’єм на темній: тон, не тінь | `A45` | **3** |
| Картки: card-in-card + стат-плитки (3 в ряд) | `A46–A47` | **3** |
| Секція-eyebrow (uppercase) + лінія-роздільник зон | `A48` | **3** |
| Чіп-плашка частки + підсилення числа (`.n`) | `A49` | **4** |
| Підняв картки → підніми хром; власний токен, не реюз surface | `A62` | **3** |
| Картки в шіті/панелі зливаються (card-on-card) → recess | `A63` | **3** |
| Баланс контрол-ряду між хедером і списком (подвійний зазор) | `A64` | **4** |
| Тогл-перемикач увімк/вимк (`.sw`) | `A52` | **4** |
| Кастом тап-селект замість нативного `<select>` | `A53` | **4** |
| Анімований повзунок у сегмент-перемикачі (slide) | `A54` | **5** |
| PWA: гола смуга знизу / overlay-бар зі склом | `A55 (+A1/A9/A10/A15)` | **1** |
| **Скло є, а блюр не працює / картинка під ним не змінюється** | `A55 (2-б)` | **1** |
| **Круглий виріз у ребрі панелі виглядає «зрізаним»** | `A87` | **3** |
| **Ореол чи кільце по краю · ореол читається як бруд на світлій** | `A88` | **3** |
| **Чіпи в рядку керування однакові, а поводяться по-різному** | `A89` | **4** |
| **Активний фільтр підсвітити — акцентом чи матеріалом** | `A89-б` | **4** |
| **Список варіантів: поповер чи нижній шіт · де шіт зупиняється** | `A90` | **4** |
| Mode-switch анімацією: ручний FLIP чи View Transitions | `A83` | **5** |
| Ліва кольорова смужка на картці «для краси» | `A84` 🔴 анти | **3** |
| Bottom-nav — ПОВНИЙ еталон (скло+капсула+іконки) | `A56 (база A54/A55)` | **2** |
| Motion-мова: драбина тривалостей + easing + снап/слайд | `A57 (звід A18/27/54/56)` | **5** |
| Sheet-меню/профіль (genSheet+grouped-cards+About+кредит) | `A58` | **2** |
| Мікро: toast / sync-pill / sparkline / confirmAction | `A59` | **4** |
| Місяць-пікер 3×4 + floor-guard + рік-пігулка (core) | `A60 (промоут A50)` | **2** |
| Дві мови глибини: well-поле (deboss) vs кнопка (lift); токен | `A65 (промоут A51)` | **3** |
| Elevation асиметричний між темами (drop≥14px / світло-зверх) | `A66 (розшир. A45)` | **3** |
| Press-механіка тапу: JS pointerdown/up; scale∝1/розмір | `A67` | **5** |
| Card-select (велика картка): persistent-DOM+opacity-ring+WAAPI | `A67.1 (база A67/A54)` | **5** |
| Прев'ю-пісочниця Claude: лише console.log/warn/error | `A68` | **1** |
| Cross-theme fused-шов: дизайнити по кожній темі; auto-override | `A69 (інстанс A39)` | **1** |
| Кільце/обведення під layout-рухом: border > box-shadow | `A70 (база A67.1/A57)` | **5** |
| Row-stagger reveal — каскадна поява рядків (generic) | `A71` | **5** |
| Scroll-linked: рівність вартості > її величина; rAF-демпфер | `A72 (база A70/A45)` | **5** |
| Offline / черга / realtime / клавіатура / форми | `B (продукт-специфіка)` | **—** |
| Хіт-таргет менший за палець / плашка ≠ тач-зона | `A73` | **5** |
| Іконки PWA з гліфа продукту; чому не міняється за темою | `A75` | **1** |
| Блок «Обслуговування»: самоперевірка версії + встановлення на екран | `A79` | **1** |
| Віддати .xlsx без бібліотек; CSV розсипався в чужій локалі | `A80` | **1** |
| Стиснути в PWA без JSZip/pako | `A81 (пара A80)` | **1** |
| Нагорода моменту «передано» (копія / експорт / шер) | `A82` | **5** |
| Дані зникли · де тестувати польові дані · чи безпечний бамп | `A85` | **1** |
| Бар/шкала запасу показує 0 при живих даних | `A86 (пара A85)` | **4** |
| Горизонтальний свайп поверх скролу — axis-lock, `passive:false` | `A44` | **5** |
| Поділки-риски шкали / вирівнювання колонок легенди | `A37–A38` | **4** |
| Подвійно-тематичний поліш: той самий токен ≠ той самий характер | `A39` | **3** |
| iOS системні CSS-пастки (tap-highlight, autofill, momentum) | `A24` | **1** |
| Range-календар: drill-режим і промоут у core | `A50` | **2** |
| Втоплене поле (well) — deboss замість lift | `A51` | **3** |
| Чек-лист deploy перед видачею колегам | `Частина C` | **1** |

-----

## §3 Повна мапа: номер → том

| # | Патерн | Том |
|---|---|---|
| `A1` | HTML head — обов’язкові meta tags | **1** |
| `A2` | PWA manifest.json | **1** |
| `A3` | Icon pack (7 файлів) + squircle-правило | **1** |
| `A4` | Cache-busting versioning | **1** |
| `A5` | FOUC prevention — inline script (канонічний) | **1** |
| `A6` | Theme dropdown 3-mode (auto/light/dark) + глянцева капсула обран | **4** |
| `A7` | CSS variables — 3-шарова тема | **1** |
| `A8` | Theme-override specificity guard | **1** |
| `A9` | `dvh` замість `vh` | **1** |
| `A10` | Safe-area-inset | **1** |
| `A11` | PWA detection — display-mode + heuristic | **1** |
| `A12` | VisualViewport — tight/roomy-vh | **1** |
| `A13` | `max-height` обмежує, не задає (діагностика) | **1** |
| `A14` | 5-layer top stack | **1** |
| `A15` | Bottom nav — 3-state padding (критичний fix) | **2** |
| `A16` | App-bar scroll elevation | **2** |
| `A17` | Bottom sheet — структура | **2** |
| `A18` | Bottom sheet — enter/exit анімація | **2** |
| `A19` | Bottom sheet — swipe-to-close (end-only) | **2** |
| `A20` | Bottom sheet — 3 способи закриття | **2** |
| `A21` | Listener registration «once-for-life» | **2** |
| `A22` | Swipe-row — рамка-пігулка (single-frame), tap=edit / swipe-left= | **2** |
| `A23` | Orientation lock — CSS overlay | **1** |
| `A24` | iOS системні CSS правила (що кожне блокує) | **1** |
| `A25` | Pinch-zoom — JS block | **1** |
| `A26` | A11y attributes | **4** |
| `A27` | Reduce-motion + no-transition при init | **1** |
| `A28` | Storage event — multi-window sync | **1** |
| `A29` | SVG icons — inline + стандартні розміри | **4** |
| `A30` | Premium polish | **4** |
| `A31` | Z-index стек | **1** |
| `A32` | Empty state | **4** |
| `A33` | Range-календар у bottom sheet (tap-based, sweep) | **2** |
| `A34` | Accent rail — ліва смужка-ідентифікатор типу на картці-рядку ✅ | **3** |
| `A35` | Кольорова семантика Lens ✅ | **3** |
| `A36` | `[hidden]` vs явний `display` — атрибут програє класу ✅ | **4** |
| `A37` | Поділки-риски під дискретним баром (без дробових гліфів) ✅ | **4** |
| `A38` | Вирівнювання колонок у багаторядковій легенді: grid + `display:c | **4** |
| `A39` | Подвійно-тематичний поліш — однакове число ≠ однаковий характер  | **3** |
| `A40` | Native-контроли у флекс-рядку — інтринзік-ширина б’ється з flex  | **4** |
| `A41` | Native picker (date/time) у standalone-PWA — color-scheme заморо | **4** |
| `A42` | Текст поверх ілюстрації / SVG → токен-скрим ✅ | **3** |
| `A43` | Inline layered SVG як фон картки / геро (переносний патерн) ✅ | **3** |
| `A44` | Горизонт-свайп поверх скрол/тап-контенту — axis-lock + `passive: | **5** |
| `A45` | Об’єм / elevation на ТЕМНІЙ темі: через тон, не тінь ✅ | **3** |
| `A46` | Card-in-card (вкладені під-картки) для двоблокової картки ✅ | **3** |
| `A47` | Стат-плитки (3 у ряд): іконка + значення + підпис ✅ | **4** |
| `A48` | Секція-eyebrow (uppercase) + лінія-роздільник зон ✅ | **3** |
| `A49` | Чіп-плашка частки + підсилення числа (число сильніше, одиниця ти | **4** |
| `A50` | Range-календар: in-sheet drill день↔місяць (calMY-свап) + антист | **2** |
| `A51` | Втоплене поле (tactile well) — афорданс «можна пожамкати»; світл | **3** |
| `A52` | Toggle-switch (`.sw`) — нативний checkbox під стильованим треком | **4** |
| `A53` | Кастом тап-селект `.pk` — заміна нативного `<select>` ✅ | **4** |
| `A54` | Анімований повзунок сегмент-перемикача (constant-width slide) ✅ | **5** |
| `A55` | PWA standalone: модель висоти документа + overlay-бар зі склом ( | **1** |
| `A56` | Bottom-nav — ПОВНИЙ еталон компонента (скло + анімована капсула  | **2** |
| `A57` | Motion-мова — драбина тривалостей + словник easing + снап/слайд  | **5** |
| `A58` | Sheet-меню / профіль-система (genSheet + grouped-cards + About + | **2** |
| `A59` | Мікро-компоненти: toast / sync-pill / sparkline / confirmAction  | **4** |
| `A60` | Місяць-пікер 3×4 + floor-guard + рік-пігулка (універсальний core | **2** |
| `A61` | Chrome (pinned-зона під app-bar) — форма та dark-elevation | **2** |
| `A62` | Підняв картки → підніми й хром (elevation-паритет); власний токе | **3** |
| `A63` | Втоплена поверхня під картками у sheet/панелі (card-on-card → re | **3** |
| `A64` | Баланс контрол-ряду між хедером і списком — подвійний зазор ✅ | **4** |
| `A65` | Дві мови глибини: ПОЛЕ=well (deboss) vs КНОПКА/ЧІП=elevation (li | **3** |
| `A66` | Elevation асиметричний між темами: drop≥14px (світла) vs світло- | **3** |
| `A67` | Press-механіка тапу: JS `pointerdown/up`, не чистий `:active`; d | **5** |
| `A67.1` | Card-select (велика картка): persistent-DOM + pseudo-opacity rin | **5** |
| `A68` | Прев’ю-пісочниця Claude: безпечні лише `console.log/warn/error`  | **1** |
| `A69` | Cross-theme fused-шов — дизайнити по кожній темі; auto-mode over | **1** |
| `A70` | Кільце/обведення під layout-driven рухом: `border` > `box-shadow | **5** |
| `A71` | Row-stagger reveal — каскадна поява рядків (generic) ✅ | **5** |
| `A72` | Scroll-linked анімація: РІВНІСТЬ вартості важливіша за її величи | **5** |
| `A73` | Хіт-експандер: тач-таргет ⟂ візуальна плашка ✅ | **5** |
| `A75` | Іконки PWA: генерація з локнутого гліфа + межі платформи ✅ | **1** |
| `A79` | Блок «Обслуговування»: самоперевірка версії + встановлення ✅ | **1** |
| `A80` | Мінімальний xlsx-writer без бібліотек (ZIP+XML вручну) ✅ | **1** |
| `A81` | `CompressionStream('deflate-raw')` — нативна заміна ZIP-бібліотеки ✅ | **1** |
| `A82` | Handoff-sweep — нагорода моменту «ПЕРЕДАНО» ✅ | **5** |
| `A85` | `localStorage` у PWA: origin ⟂ контейнер + три механізми втрати ✅ | **1** |
| `A86` | Індикатор запасу не показує нуль, коли ресурс є ✅ | **4** |
| `A83` | View Transitions API — перший кандидат для mode-switch, не ручний FLIP ✅ | **5** |
| `A84` | АНТИ-ПАТЕРН: декоративний left-accent-rail 🔴 | **3** |
| `A87` | Галтель вирізу: розрив кривини читається як злам; G1-неперервність ✅ | **3** |
| `A88` | Ореол ≠ внутрішнє кільце; ореол обов'язково роздільний по темах ✅ | **3** |
| `A89` | Граматика знаків рядка керування (+`A89-б` актив = матеріал, не акцент) ✅ | **4** |
| `A90` | Поріг поверхні поповер ↔ шіт; шіт іде від нижнього ребра екрана ✅ | **4** |
| `B1` | Drive Lens — read-write контекст (offline / sync) | **4** |
| `B2` | VisualViewport — клавіатура в bottom sheet 🆕 | **4** |
| `B3` | Input attributes — форми 🔧+🆕 | **4** |
| `B4` | Numeric fuel gauge — 12 поділів 🆕 (Drive Lens) | **4** |
| `B5` | QR Lens — QR-код розмір + Tab-3 layout (Phase 3) 🆕 (QR Lens) | **4** |

-----

## §4 Ланцюги залежностей

Розпил рознiс кілька зв'язаних рецептів по різних томах. Ці читаються **разом**:

| основний | база / супутнє | томи |
|---|---|---|
| `A56` bottom-nav еталон | `A54` повзунок · `A55` PWA-смуга | 2 ← 5, 1 |
| `A57` motion-мова (звід) | `A18` sheet · `A27` reduce-motion · `A54` · `A56` | 5 ← 2, 1 |
| `A67.1` card-select | `A67` press · `A54` | 5 (усі тут) |
| `A72` scroll-linked | `A70` кільце · `A45` матеріал | 5 ← 3 |
| `A66` elevation-асиметрія | `A45` (розширює) | 3 (обидва тут) |
| `A69` cross-theme шов | `A39` (інстанс) | 1 ← 3 |
| `A6` дропдаун теми | `A5`/`A7`/`A8` тема-інфраструктура | 4 ← 1 |
| `A73` хіт-експандер | `A67` press-механіка | 5 (обидва тут) |
| `A75` іконки PWA | `A2` manifest · `A3` icon pack | 1 (усі тут) |
| `A79` обслуговування | `A2` manifest · `A11/A12` режими · `A75` іконки | 1 (усі тут) |
| `A80` xlsx-writer | `A81` стиснення | 1 (обидва тут) |
| `A82` handoff-sweep | `A70` композит · `A67` press | 5 (усі тут) |
| `A85` сховище | `A86` індикатор · `A79` самооновлення | 1 · 4 |
| `A87` галтель вирізу | `A55 (2-б)` backdrop-root · `A88` край | 3 ← 1 |
| `A88` ореол ≠ кільце | `A39` дуал-тема · `A45` тон-елевація | 3 (усі тут) |
| `A89` граматика знаків | `A90` поріг поверхні · `A51`/`A65` well | 4 ← 3 |
| `A55 (2-б)` смерть скла | `Lens_glass_FINDINGS.md` §9 (виріз) · `A87` | 1 ← products |

-----

## §5 Глобальний контекст (не дублюється в томах)

**Джерело.** Консолідовано з: Drive Lens `concept_v1.2 §14` (46 гранулярних пунктів, перенесених з QR Lens),
wsd Кластери 7/8/9 (+7.10, 8.2). Source-of-truth точного коду:

- стандартні патерни → `QR_Lens_preview_batch22.html` (grep по назві патерну);
- swipe-пігулка + sheet enter/exit → `Drive_Lens_preview_batch10_2.html`.

**Тестова база.** iPhone XS iOS 18 PWA (vh≈812 roomy / Safari≈762 default / Safari згорнутий≈715 tight); вторинна — iOS 26.

**Маркування.**

- ✅ **Універсальне** — переносимо у будь-який Lens 1:1.
- 🔧 **Адаптовано** — той самий патерн, інші параметри/назви під продукт.
- 🆕🔒 **Продукт-специфічне** — не тягнути в інші Lens без перегляду. Винесено в **Частину B** (хвіст тому 4).

**Власність (анти-дубль із wsd).** iOS/PWA/UI патерни живуть ТУТ. wsd на них лише посилається.
Не дублювати — крос-посилання за назвою.

**Лоток нових записів** — `Lens_cookbook_delta_running.md` (вічний, порожній = норма).

-----

## §6 Борги (не сесія B)

| що | куди | коли |
|---|---|---|
| `A68` (пісочниця Claude) — це **метод роботи**, не патерн продукту | `Lens_sandbox_manifest.md` | сесія E |
| Частина B (B1/B4 Drive · B5 QR) — продуктове; B2/B3 насправді універсальні | розселити | сесія E |
| Частина C (deploy чек-ліст) — це **процес** | wsd Кластер 10 | сесія C |
| «Правило двох осей» + реєстр екземплярів | wsd 2.4 (кандидат уже в буфері) | сесія C |

> живе доки: назавжди (вічне, wsd 1.8) — значення матеріальності StockCheck

# StockCheck · MATERIALITY valuesLOCK (пресет γ′)

**Статус:** BENCH-LOCKED — device-підтверджено на stage-bench v2 (iPhone, обидві теми).
Pending: **in-product verify** після порту в `StockCheck_port_b15_2.html`.
**Джерело:** `StockCheck_materiality_stagebench_v2.html` · preset=G + ручні оверрайди → **γ′**
**Канон-опора:** A45 (+manifest) · A64 · A65 · A66/A66.1 · FINDINGS §3b/§3c/§3d/§3e

---

## 0. Що саме залочено (ставка)

**γ′ = таця несе групу + контролі мають власний матеріал.**
Не β (таця несе все, контролі тихі) і не α (тільки контролі, без таці).

Device-аргумент (слова арбітра): *без таці чіпи/кнопки відчуваються загубленими, «не мають свого дому»; з тацею вони одночасно втоплені і наче мають скляну поверхню.*

**Перегляд рішення v1.** У bench v1 таця була «знята зі ставок» — це рішення прийнято на **битій реалізації** (порушений A65-спліт: темний wash на світлій темі). v2 дав чесний тест → ставка повернулась і виграла.

---

## 1. МАРШРУТ

```
route = T   (власний матеріал контролям)
tray  = ON
netbtnPlate = ON     ← §3e закрито: попри власний якір (ntile), netbtn плашку ЗБЕРІГАЄ
island: op .82 · blur 12 · sat 170%   ← НЕ ЧІПАНО (FINDINGS §4-preset locked)
```

---

## 2. ВАЖЕЛІ — L і D це РІЗНІ НАБОРИ (A45-manifest, правило 2)

| | СВІТЛА | ТЕМНА |
|---|---|---|
| набір | `grad · dropB · dropA · fillD · bord` | `grad · ridge · edge · fillD · bord` |
| носій об'єму | drop-тінь | tone-lift (ridge+edge) |
| drop | є | **важеля НЕМА взагалі** (мертвий на OLED) |

`srch` — окрема мова (**deboss**) в обох темах: `wellTop · wellBot`, drop/ridge не застосовується.

### СВІТЛА
| родина | grad | dropB | dropA | fillD | bord |
|---|---|---|---|---|---|
| bc   | 10 | 3 | 14 | 0 | 100 |
| chip | 10 | 3 | 14 | 0 | 100 |
| act  | 10 | 3 | **18** | 0 | 100 |
| srch | 0 | — | — | 0 | 100 | `wellTop=20 · wellBot=0`

### ТЕМНА
| родина | grad | ridge | edge | fillD | bord |
|---|---|---|---|---|---|
| bc   | 10 | 12 | 20 | 4 | 100 |
| chip | 11 | 14 | 20 | 3 | 100 |
| act  | **16** | **17** | **22** | 3 | 100 |
| srch | 0 | — | — | −5 | 100 | `wellTop=55 · wellBot=12`

---

## 3. ТРИ РІШЕННЯ, ЯКІ ТРЕБА ЗБЕРЕГТИ ЯК ПРИНЦИПИ (не як числа)

### 3.1 `dropB=3` — контактна тінь, а не lift. **Обґрунтований виняток з A66.**
A66 каже: lift на світлій хоче `blur ≥14`. Тут **3**.
Причина: предмет **у западині** (таця) відкидає **коротку контактну** тінь. Довга тінь у ямі = «висить над ямою» — саме дефект γ у bench v1.
→ **Правило:** amplitude drop-тіні підпорядкована стану вкладеності, не абсолютній шкалі A66. У recess `dropB` падає, `dropA` лишається (сила є, розмиття нема).

### 3.2 `chipOverride = ON` — чіп НЕ тихіший за сусідів
Авто-зчеплення стенду знижувало drop чіпа при вмиканні таці. Арбітр зняв: `bc / chip / act` зведені в **одну мову** (`grad10 · dropB3 · dropA14`).
→ **Правило:** recess застосовується до **всієї групи однаково**; всередині таці немає «головних» і «другорядних» контролів. Різниця виправдана лише при **різному контексті** (див. 3.3).

### 3.3 `act` підвищений — бо живе ПОЗА тацею
`act` (netbtn + •••) — єдина родина на **голому склі**, не в западині. Тому несе матеріал сам:
- світла: `dropA 18` (проти 14)
- темна: `grad 16 · ridge 17 · edge 22` (проти 10–12 / 12–14 / 20)

→ **Правило:** амплітуда матеріалу — функція **ґрунту**, на якому елемент лежить (западина vs голе скло), а не функція родини.

---

## 4. ЗАЗОРИ (спільні на обидві теми — геометрія в проді одна)

```
gapBC    = 10    (пігулки → чіпи)      чек §3c: 10 / потрібно 2  ✓
gapCH    = 10    (чіпи → пошук)        чек §3c: 10 / потрібно 2  ✓
tierPadT = 10
tierPadB = 12
```
Запас великий, бо `dropB=3` (див. 3.1) — вимога «≥⅔ blur» майже не тисне.
**Зазори підняті проти b15_2** (`8/8/9/10`) — матеріал з'явився, дихання потрібне.

---

## 5. ТАЦЯ — за A65-сплітом, per-theme

```
СВІТЛА : tone =  0   inset = 14   rim =  0     ← западину несе ТІНЬ
ТЕМНА  : tone = 23   inset = 17   rim = 10     ← западину несе ТОН
спільне: rad = 22    pad = 8      (бічний margin = 14 − pad = 6px)
```

**Це відтворення A65-спліту наосліп** — арбітр вийшов на нього сам, без підказки. Головне підтвердження, що спліт реальний, а не теоретичний.

### 5.1 `trayRad = 22` — СИМПАТИЧНИЙ радіус, свідомо обраний над концентричним

Концентричний був би **18** (`island 24 − gap 6`).
Обрано **22** ≈ radius острівця (24).

Аргумент арбітра (уточнений): у **колапс-стані** ярус — це майже весь острівець; два різні радіуси читались би як «кільце в кільці» + зайві шари. 22 дає **одну м'яку форму**.

⚠️ **Поправка до премісу для канону:** таця **не зливається** з острівцем навіть у колапсі — вкладеність зберігається (**6px з боків**, **12px знизу** = `tierPadB`). Обґрунтування — саме **мова форми**, не збіг геометрії.

→ **Кандидат у Cookbook:** *концентричний радіус* (внутрішнє = вкладений окремий предмет, рівний зазор) vs *симпатичний радіус* (внутрішнє = та сама форма/матеріал). Обирається за наміром, не за формулою.

---

## 6. ГОТОВІ CSS-РЯДКИ ДЛЯ ПОРТУ (обчислено зі стенду — звіряти після порту 1:1)

Бази (вже є в b15_2):
`--b-bc` = `surface-2` L / `surface-3` D · `--b-chip` = `#f7fbf9` / `#223029`
`--b-srch` = `color-mix(in srgb,var(--bg-soft) 82%,var(--card))` / `color-mix(in srgb,var(--card),#000 24%)`
`--b-act` = `surface-2` L / `surface-3` D · `bord=100` → `border: 1px solid var(--border)`

### СВІТЛА
```css
/* bc / chip — фон однаковий */
background: linear-gradient(180deg,
  color-mix(in srgb, var(--b-bc) 90%, #fff),
  color-mix(in srgb, var(--b-bc) 90%, #000));
box-shadow: 0 1px 2px rgba(20,50,42,.056),
            0 1.3px 3px rgba(20,50,42,.140);

/* act (dropA 18) */
box-shadow: 0 1px 2px rgba(20,50,42,.072),
            0 1.3px 3px rgba(20,50,42,.180);

/* srch — deboss */
background: var(--b-srch);
box-shadow: inset 0 1px 2px rgba(20,50,42,.200);

/* tray */
background: transparent;
box-shadow: inset 0 2.0px 5.9px rgba(20,50,42,.154);
```

### ТЕМНА
```css
/* bc — grad10 fillD+4 */
background: linear-gradient(180deg,
  color-mix(in srgb, color-mix(in srgb,var(--b-bc) 96%,#fff) 90%, #fff),
  color-mix(in srgb, color-mix(in srgb,var(--b-bc) 96%,#fff) 90%, #000));
box-shadow: inset 0 1px 0 rgba(255,255,255,.120),
            inset 0 -1px 0 rgba(0,0,0,.200);

/* chip — grad11 fillD+3 */
box-shadow: inset 0 1px 0 rgba(255,255,255,.140),
            inset 0 -1px 0 rgba(0,0,0,.200);

/* act — grad16 fillD+3 */
box-shadow: inset 0 1px 0 rgba(255,255,255,.170),
            inset 0 -1px 0 rgba(0,0,0,.220);

/* srch — deboss, fillD −5, grad 0 */
background: color-mix(in srgb, var(--b-srch) 95%, #000);
box-shadow: inset 0 2px 5px rgba(0,0,0,.550),
            inset 0 -1px 0 rgba(255,255,255,.120);

/* tray — tone несе */
background: color-mix(in srgb, transparent 77%, var(--card));
box-shadow: inset 0 2.4px 2.7px rgba(0,0,0,.374),
            inset 0 -1px 0 rgba(255,255,255,.100);
```

> ⚠️ Вкладений `color-mix` (темна, grad+fillD) — перед портом **спростити в один шар** або порахувати кінцевий hex. Два рівні `color-mix` у `background` під `backdrop-filter` — небажаний перф-ризик (див. §7).

---

## 7. ПЕРФ (frame-gap, стенд, baseline 17.0 ms)

| тема | Δ vs baseline | вердикт |
|---|---|---|
| СВІТЛА | **+2.0 ms** | ok |
| ТЕМНА | **+4.0 ms** | ok, але **вдвічі дорожча** |

Темна дорожча через `color-mix` тону таці + inset **усередині** `backdrop-filter`.
→ **У порт-чеклист:** перезаміряти frame-gap **на темній** після порту; блюр вже на стелі 12 (Cookbook), запасу нема.

---

## 8. ЩО НЕ ЗАКРИТО

- **in-product verify** — стенд ≠ продукт (wsd: канон лише після device-тесту в реальному продукті)
- **порт структурний, не токенний**: у `b15_2` елемента `.tray` **не існує** → правка розмітки ярусу
- **скрол-дисонанс** (FINDINGS) — окрема задача, цим локом НЕ вирішена
- **Cookbook island A-entry** — після in-product verify
- кандидати в канон: §3.1 (recess-виняток з A66) · §3.3 (амплітуда = функція ґрунту) · §5.1 (симпатичний vs концентричний радіус)

---

## 9. ДРАБИНА ТОНІВ ТЕМНОЇ ТЕМИ — заміряно, не виводити заново

### 9.1 Драбина тонів, темна тема — заміряно з `StockCheck_port_b28.html`

> Джерело: р.84-90 (токени), р.494-500 (`.card`), р.489 (`--cardBg`), р.756-764 (FILL rows).
> L\* — CIE, рахований із hex. **Не виводити заново щосесії.**

| токен | hex | L\* | ΔL\* до `--bg` |
|---|---|---|---|
| `--bg` | `#0d1512` | 5.2 | — |
| `--bg-soft` | `#151e1a` | 10.2 | 5.0 |
| `--card` | `#18221e` | 12.5 | 7.3 |
| `--surface-2` | `#1c2723` | 15.3 | 10.1 |
| `--surface-3` | `#212d28` | 18.2 | 13.0 |
| `--border-subtle` | `#222e29` | ~18.5 | 13.3 |
| `--border` | `#2a3833` | 22.9 | 17.7 |

**Похідні поверхні (не плаский токен — рахувати не можна, тільки брати звідси):**

| поверхня | що це | L\* |
|---|---|---|
| Home `.card`, dark | `linear-gradient(180deg,#1a251f,#161f1b)` + ridge .05 / edge .22 | **13.0 → 10.8**, ΔL\* до підлоги ≈ **6.7** |
| FILL `.brand`, dark | `color-mix(--bg 25%, --card)` = `#151f1b`, **без рамки й рельєфу** | **10.3**, ΔL\* до підлоги ≈ **5.1** |

**Три висновки, що випливають із драбини:**

1. **У b28 картка ніколи не стоїть на `surface-2`.** Home = `~--card` (12.5) на `--bg` (5.2).
   Будь-який стенд, що ставить групу на `surface-2`, вже на 2.8 пункти вищий за продукт.
2. **Край сильніший за тон учетверо.** Рамка `--border` дає ΔL\* 17.7 проти підлоги;
   найбільший тональний крок між сусідніми поверхнями — 7.3. У темній темі форму
   визначає край, тон лише підтримує.
3. **Опускай підлогу, не піднімай вкладене.** Підняття контейнера з'їдає headroom
   у всього, що на ньому лежить: плита тира (З-8) і тінт вибраного рядка FILL (§5 самері) —
   два незалежні підтвердження того самого закону.

> *Походження: `Lens_governance_delta_F1_2.md` Блок 2, сесія F1.2. Перенесено 04.08.2026, вузол G1.*

---

## 10. ГЕОМЕТРІЯ ЖУРНАЛУ — пресет P3, єдине джерело

**Статус:** DEVICE-LOCKED (прохід Konst 01.08.2026, v2) · порт у `b29.6` закритий заміром
лінійкою `StockCheck_port_b29_5_RULER.html`, XS/375, обидві теми.

**Джерело — рядки 385–388 стенда `StockCheck_journal_stagebench_v3.html`:**

```js
 p3:{name:'P3 твій 01.08',
   SEG:{dataMix:'real',comp:'ring',ringSide:'left',addrSplit:'on',nameFit:'full',metaLine:'city',addr:'ext',tierHome:'tail',tierStyle:'plate',tierLab:'slash',cnt:'off',chev:'on',ringNum:'n',hdrPos:'in',hdrMode:'sticky',hdrBg:'well',hdrCase:'caps',wd:'on',divider:'line',lastDiv:'on',wrap:'card',cardFloor:'s2',clr:'ring'},
   NUM:{rowH:44,padX:14,gap:11,nameSz:14,metaSz:11.5,rowPadY:10,ringSz:28,hdrSz:11,hdrGapT:10,hdrGapB:7,hdrPadY:3,stickyTop:0,divL:17,divR:17,chevSz:15,rows:7,groups:4,clrSz:20},
   INK:{light:{valL:100,mutL:90,bdA:73},dark:{valL:100,mutL:90,bdA:82}}},
```

**Дві пастки, які цей запис знімає назавжди:**

1. **`var NUM` (р. 366–368) — мертвий дефолт.** Стенд на старті виконує
   `applyPreset('p3')` (р. 754), тобто перезаписує `NUM` **до першого кадру**.
   Греп за `NUM:{rowH:44` дає **п'ять** входжень (р. 377/380/383/387/392 = P0…P4);
   перше з них — P0, не P3. Самері F2.6 склало таблицю звірки проти нього
   і мало не відкотило геометрію до стану, який на пристрої ніколи не схвалювався.
   *(Клас помилки — wsd §3.11.)*
2. **Стенд не можна архівувати, поки числа живуть лише в ньому.** Цей розділ і є
   винесенням: після нього `journal_stagebench_v3.html` втрачає статус
   «не архівувати» — геометрія тепер має дім у `products/`.

**Еталон вирівнювання (заміряно, не вгадано):** ширина групи журналу вирівнюється по
**картці ДОШКИ** (14…14), **не** по острову (12…12). `.jr` і `.jr-grp` бічних полів не
несуть — група успадковує 14 від `.list`. Роздільник 14/14, шеврон 15.

> *Походження: п. 30 дерева хвостів, сесія F2.7. Перенесено 04.08.2026, вузол G1.*

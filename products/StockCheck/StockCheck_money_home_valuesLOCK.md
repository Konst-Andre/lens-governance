> живе доки: назавжди (вічне, wsd 1.8) — значення грошового блоку Home

# StockCheck / Фармастор — Money-Home valuesLOCK

**Статус:** BENCH-LOCKED (stagebench v2, device-locked XS + 15Pro, обидві теми) — pending in-product device verify після порту.
**Джерело:** `StockCheck_money_home_stagebench_v2.html`
**Ціль порту:** `StockCheck_port_b19.html` (= той самий Фармастор, ребренд).

---

## 1. Що це
Показ **грошового розриву vs MSL** прямо на філ-екрані: скільки ₴ треба довезти (плюс) або перезатарено (мінус). Дві точки показу («дім»):
- **Родина A — бейдж-дім:** біля верхніх бейджів **OTC / IW** — сума по категорії.
- **Родина B — акордеон-дім:** у хедері кожного **бренд-акордеона** — сума по бренду. Бейджа там НЕ треба (бренд сам каже категорію).

## 2. Арифметика (реальна, по-SKU)
```
order(SKU)  = MSL_ярус − порахував        // MSL_ярус = поле A/B/C/D за ярусом точки для категорії SKU
money(SKU)  = order(SKU) × p              // p = Custom.Price (поле "p" у MSL[])
категорія₴  = Σ money(SKU) по c ∈ {OTC,IW}
бренд₴      = Σ money(SKU) по бренду
```
- Знак: **+** = недозавантаження (треба закупити) · **−** = overstock (перезатарено). Показуємо обидва (мінус, як в оригіналі Excel: N=MSL−Залишок, O=N×Price).
- Жива: перераховується при кожній зміні лічби SKU.
- Реальні орієнтири (ярус C/D, порахував=0): OTC +13 849 · IW +7 079 · Нурофен +8 275 · Стрепсілс +3 727 · Гавіскон +1 847.

## 3. Форма — ВИТІСНЕНА (О-49, 13.08.2026)

> 🔴 **Стара §3 описувала `.money.h-pill` — одну знакову пілюлю. У продукті її немає.**
> Замір на `StockCheck_port_b32_2_s16_money_net.html` (13.08.2026): `h-pill` — **0 входжень**,
> `.money` лишився **лише у двох пояснювальних коментарях**, як ім'я померлої форми.
> Тримати мертвий CSS у файлі з іменем `valuesLOCK` небезпечно: це рівно та поверхня,
> куди дивляться при наступному тюнінгу, і вона віддала б значення, яких нікуди прикласти.

**Чинна форма — спліт-капсула `.mny`** (О-49, стенд `StockCheck_money_stagebench_v2.html`,
device-судимо s15c, порт b32.2 device✓ 13.08.2026).

### 3.1 Геометрія (обидва доми)

| важіль | значення | де |
|---|---|---|
| `mPadX` / `mPadY` | 3 / 3.5 | половина капсули |
| `mRad` | 9 | `border-radius` капсули |
| `mFs` | 11 | `font:700 11px/1` |
| `mDivW` | 1 | ширина `.mdiv` |
| `mBw` | 1 | рамка, **обидва доми однаково** |
| `faGap` / `faPairGap` | 4 / 3 | ряд A (бейджі OTC/IW) |
| `fbGap` | 5 | ряд B (бренд-акордеон) |
| `bcntMin` | 34 | базовий `.bcnt` |
| `bcntBd` | 82 | рамка лічильника, **тільки темна**, обидві гілки |

**Ряд A:** `faRows 1` · `faWrap nowrap` · `faAlign center`. **Ряд B:** `money-first` (= порядок DOM).

### 3.2 Матеріал — LIGHT

```css
.mny{display:inline-flex;align-items:center;font:700 11px/1 var(--font);
  font-variant-numeric:tabular-nums;letter-spacing:.1px;white-space:nowrap;
  flex-direction:row-reverse;padding:0;overflow:hidden;
  border:1px solid transparent;border-radius:9px;
  background:var(--card);box-shadow:0 1.5px 1.5px rgba(20,50,42,.16)}
```
Половини — `fillMix 7%`. Рамка — `--muted 14%`, **`bdFA = bdFB`** (доми не розходяться).
`.mdiv` — `var(--border-subtle)`.

### 3.3 Матеріал — DARK

`.mny` — `--surface-3 24%` (= `fillMix 12 × 2`) + ridge `rgba(255,255,255,.14) → 7px`,
`box-shadow:none`. Половини — 12% від `hsl(6 42% 17%)` (мінус) / `hsl(163 37% 14%)` (плюс).
Рамка — `--muted 37%`. `.mdiv` → `var(--border)`.

### 3.4 Маркер дому — на самому елементі (Г-4)

Доми пишуться **компаундом**: `.mny.fa` і `.mny.fb`, ніколи `.fa .mny`.
Розмітка несе клас сама: `class="mny fa empty"`, рядок рендера — `var bmCls='mny fb'+…`.
Порушення цього правила коштувало півдня тюнінгу повзунка, не під'єднаного ні до чого.

### 3.5 Що НЕ переїхало зі стенда

**Оснастка стенда (жила тільки в комбінаторі):** `magn` · `abbr` · `caseMode` · `iwCase` ·
`faPairs` · `faStretch` · `bcntState` · `dchip*`-важелі · `addrCase` · `roFlag` · `devW` · `zoom`.
**Мертве за фактом:** `mutL 46` (жив лише у `shape:lead`) · `valL 100` (= чистий `--base`) ·
половинні рамки `.mh` (у `split` інертні).

**Токени знаку** (лишаються чинними): `--mpos` L `hsl(162 82% 27%)` / D `hsl(163 52% 60%)` ·
`--mneg` L `#c0392b` / D `#e07a6f` · `--negsoft` L `#eab8b2` / D `hsl(6 42% 22%)`.
Темний `#e07a6f` — **навмисний хардкод**, не `var(--crit)`.

---

## 4. Вирівнювання — лок ЖИВИЙ, підтверджено заміром

Дім у бренд-хедері «пливе» через різну ширину лічильника SKU (0/20 vs 0/7).
**Фікс:** фіксований слот під найширше «NN/NN».

**Замір b32.2 (13.08.2026):** `.bhdr .bcnt{min-width:56px}` — **1 входження, живе**.
Базовий `.bcnt` при цьому має `min-width:34px` — це **різні поверхні**, не конфлікт:
34 = лічильник узагалі, 56 = слот у бренд-хедері, де стоїть дім грошей.

> ⚠️ **Поправка до П-5 і до §3 самері `G1_HYGIENE`.** Обидва називали цей лок «мертвим»
> і шукали «56» у `materiality §4`, де його справді немає. Число живе **тут**, у §4 цього
> файлу, і воно **чинне**. Нічого не витирати. Джерело помилки — адресація по пам'яті
> замість grep (wsd 12.1).

## 5. Скоуп акордеона
Дефолт **Бренд** (одиниця згортання; згорнув → бачиш тотал бренду). Опції: Під-секція / Обидва. Сума на під-секції рендериться **лише де секцій ≥2** (напр. Гавіскон: Таблетки/Суспензія). Пласкі бренди в режимі «тільки Під-секція» лишаються без цифри — навмисно (тому Бренд = must-have).

## 6. Феасибіліті
Все in-app: `p` (ціна), `A/B/C/D` (MSL ярусу), `c` (категорія) вже у `MSL[]`. Нічого доганяти з Excel.

## 7. Хвости / наперед
- Контур (outline) поки на фіксованому A45-коректному матеріалі — якщо колись оберемо контур, дати йому per-theme важелі.
- Stagebench-канон: додати **стейдж-зум +/−** та чіткий **hide-panel** (обов'язкові в стенді) — у `Lens_stagebench_manifest`.

> живе доки: гілку gh-push/protocol-v1 змерджено в main і B63 стартував (тоді — в archive/summaries)
> Сесія GH1 · 16.09.2026 · тема: запис у GitHub із чату Claude · наступна: QR Lens B63

# Самері GH1 — ПУШ З ЧАТУ

## §0 ВІДКРИТЕ
1. **Змерджити** `gh-push/protocol-v1` → `main` у lens-governance (кнопка на GitHub, робить Konst). До мерджу новий чат НЕ знайде протокол через `main`-raw.
2. **Вставити блок GITHUB** в інструкцію Project — у **кожному** акаунті Claude: одразу після «ДЖЕРЕЛА ПРАВДИ», перед «СТАРТ СЕСІЇ» (текст блоку — в чаті GH1; токен у самері НЕ пишемо).
3. (опційно) **Ruleset на `main`** обох репо: заборона deletion + force-push (Ш4).
4. **B63** — QR Lens, анімації табів (стартове повідомлення §6).
5. Токен діє **до 16.10.2026** → за кілька днів до — новий (протокол §7).

## §1 Що зроблено
| крок | результат |
|---|---|
| веб-розслідування | офіційний віддалений GitHub MCP у claude.ai не працює (лише OAuth через GitHub App); custom connector — тільки OAuth, без Bearer; Render free засинає 15 хв. Мій попередній варіант B («сервер з токеном як секретом») **виправлено як неточний** |
| Grok | GitHub-конектор з OAuth від xAI з 06.05.2026 + BYO MCP. Різниця — хто зробив конектор, не технологія |
| тест читання | 200, ліміт 5000/год, строк токена з заголовка GitHub |
| тест запису AE-Simulator | гілка 201 → коміт `08686a9` 201 → читання назад → гілку видалено 204; `main 4ca3b52` без змін |
| пакет у lens-governance | коміт **`9b5a9c4`** → гілка `gh-push/protocol-v1` (ahead 1, 3 файли), `main d3fe019` без змін, PLAN-ID `cb16e3` |

## §2 Файли (живуть у репо lens-governance, `kernel/`)
| файл | що |
|---|---|
| `Lens_claude_github_push.py` | пуш одним атомарним комітом; `--dry-run` → `--confirm PLAN-ID`; без видалень/force; блок токена у файлах; блок `AE_WORK_index_X*` |
| `Lens_github_push_protocol.md` | токен, де живе, запобіжник 4 шари, П-GH1, сценарій, переїзд акаунтів |
| `Lens_INDEX.md` | +2 рядки маршруту (§1 і «Код і інструменти») |
Гейт `--gov`: база ✓6·⚠40·✗89 = з пакетом ✓6·⚠40·✗89 (нових провалів 0; старі ✗ — не з цієї сесії).

## §3 Правила, що народились у сесії (14.29 — повним текстом)
> **П-GH1 (запис у GitHub з чату).** Claude пише в GitHub лише через `Lens_claude_github_push.py`
> (тягне його raw щосесії, як `Lens_validate.py`). Жодного запису на старті сесії, «заодно» чи з власної ініціативи.
> Кожен запис — окремий хід за циклом wsd: **мікроскоп пушу** (репо · гілка · кожен файл: НОВИЙ/ЗМІНА +/− · PLAN-ID з dry-run)
> → явне «так» Konst у цьому чаті → `--confirm PLAN-ID`. Питання «пушимо?» — завжди окремим віджетом, не приховано в тексті.
> Типова ціль — нова гілка; `main` — тільки коли Konst назвав саме main. Видалення файлів/гілок і force-push — лише руками Konst на GitHub.
> Робочий index `AE_WORK_index_X*.html` не пушимо (В-31/В-94). Токен — лише в інструкції Project; у файли, самері, пам'ять, репо — ніколи.
> Токен прострочився (401) → сказати Konst, нічого не обходити.

> **П-GH2 (токен ⟂ акаунт).** Fine-grained PAT належить GitHub-акаунту → той самий токен вставляється в інструкцію Project
> кожного акаунта Claude. Зміна токена = заміна рядка в УСІХ інструкціях + Revoke старого.

> **В-95 (уточнення П-GH1 щодо Олі).** Проєкт і інструкція — на акаунті Konst; Оля отримує продукт, не токен і не репо. Пункт «видалити токен перед передачею» — скасовано.

## §4 Навчальне для Konst (гілки)
`main` — основна лінія, «чистовик». Гілка — копія `main` на момент створення, «чернетка» поруч: змінюється окремо, `main` не чіпає.
«Нова гілка від main» = безпечно: чернетка стартує з актуального стану. Мердж (Pull request → Merge) переносить зміни в `main`;
непотрібну гілку — Delete branch. Конфлікт буває, лише якщо `main` за цей час змінили в тих самих рядках.

## §5 Дім цього самері
Project (поруч з QR_Lens_session_summary_B62_HSO_LOCK.md). У репо НЕ пушимо. Гейт G10: оголосити в `Lens_INDEX §5`, якщо QR Lens тримає стелю 2 — старше самері в архів.

## §6 Стартове повідомлення (скопіювати в новий чат)
```
Мова — українська. Першим питанням — чи оператор за ПК.
QR Lens · B63 · порт анімацій іконок табів. Оператор з телефона, wsd-старт (INDEX/черга) не ганяти.
База — QR_Lens_preview_batch62_haptic_v2.html (B62 HSO LOCK, самері QR_Lens_session_summary_B62_HSO_LOCK.md).
Донор — github.com/Konst-Andre/Grok_Animation/public/nav-icon-animations.html (md5 97461153…); решту репо (React/src/scripts) ігнорувати.
Рішення Konst: наші SVG-іконки лишаються, беремо рух Grok (keyframes компас/плюс/QR/шари + механіка .play + reduced-motion).
Не брати: капсулу Grok (дубль placeCap B26), панелі, тему, keydown. Класи з префіксом ni- (.ring/.box зайняті), id градієнта/кліпу унікалізувати.
Хук — setTab() (єдина точка входу); анімацію вішати на svg-дитину .nav-i, не на кнопку (HSO-overlay і прес .96 стабільні).
Наші іконки під рух розбити на групи (компас: коло+стрілка; плюс: рамка+хрест; шари: 3 path; QR — узгодити, що рухаємо).
Відкрите рішення: перегравати анімацію при повторному тапі активного табу (HSO там мовчить) — так чи ні.
Цикл: план → мікроскоп → підтвердження → код. Template не чіпати.
Новинка GH1: запис у GitHub з чату працює (П-GH1, kernel/Lens_github_push_protocol.md) — лише з мікроскопом і моїм «так».
Самері попередньої сесії: Lens_session_summary_GH1_PUSH_Z_CHATU.md.
```

> живе доки: тексти влито скриптом G-F3 і запушено — тоді `archive/summaries/Lens_gov/`

# G-F2 · `К2-1` wsd група 1 з ~4 (+ `К1-1` тим самим проходом) · `Work_Standard.md` · 17.09.2026

База: `main` = `f35a2e7` · `--gov` ✓11 ⚠39 ✗93 · G16 wsd: 36 з 68.
Прочитано цілком (12.19): `1.2` 4163 · `1.3` 1928 · `1.4` 1880 · `1.5` 1364 · `1.6` 1730 · `1.7` 1510 · `1.12` 1901 · `1.13` 1344 · `2.1` 3151 · слабкий ✓ `1.11` 1661 B.
Шаблон вироку — `GE2_k2_gov_texts_v1.md` §1. Вирок делеговано Konst («виріши самостійно»), тексти перевірено на живих даних до запису (§2).

## §1 Вироки

| правило | вирок | тригер |
|---|---|---|
| `1.2` | детектор | явний у тілі |
| `1.3` | детектор | є `**Тригер:**` |
| `1.4` | `K2:n/a` | — |
| `1.5` | детектор | + рядок |
| `1.6` | детектор | + рядок |
| `1.7` | детектор | явний у тілі |
| `1.11` *(слабкий ✓)* | детектор | + рядок |
| `1.12` | детектор | + рядок |
| `1.13` | детектор | + рядок |
| `2.1` | детектор | явний у першому реченні |

## §2 Перевірка чернетки на живих даних (що змінилось проти показаного в чаті)

1. `1.2` — регекс `[█░]{12}` дав би хибний ✗ на формі маркера з інструкції Project (`▓`) → `[█▓░]{12}`.
2. `1.3` — «останній `## `» дав би ✗ на GA (`## Погашено` після §6); 20/20 самері в Project мають заголовок зі «Стартове повідомлення» (§6 або §7) → перевіряється наявність, не позиція.
3. `1.5` — «`🔒` без `device✓`» дав би ~49 хибних ✗ (`EquipLens_MASTER_LOCK.md`: 49 `🔒`, 0 `device✓`) → замінено на межу CHERGA ⟂ LOCK (`Lens_INDEX` §5: рядок в обох — дефект).
4. `1.6` — суфікс версії дав би ✗ на кожному самері → самері виключено з перевірки імені.
5. `1.7` — у репо жодного `.html` для звірки форми → `rel=["']?manifest`.
6. `1.12` — `Lens_stagebench_manifest.md` має 2 `clipboard`, 0 `execCommand`; фолбек `execCommand('copy')` лишено як легальний.
7. Маркер `K2:n/a` — **латинська** `K` (`0x4b`), як у регексі G16. Кирилична `К2` проходила б лише через хибний ✓ `G16-1`.

## §3 Дрейф, знайдений прочитанням (дім — самері G-F §2)

- **Ф-1 · `1.4` ⟂ практика:** обов'язкові секції правила («Що зроблено» · «Уроки» · HIGH/MEDIUM/LOW) у живих самері не використовуються; практика — `§0 ВІДКРИТЕ · §1 · §2 · §6`. Тому `n/a`, а не детектор (хибний ✗ на кожному самері, 12.12).
- **Ф-2 · `1.3` ім'я `<Product>_session_summary_BatchXX.md` ⟂ практика** `<Продукт>_session_summary_<id>_<ТЕГ>.md`.
- **Ф-3 · форма маркера `1.2`:** правило — `inline-code`, `█`/`░`; інструкція Project і відповіді — без бектиків, з `▓`. Той самий клас, що «колізія інструкції Project» (GE §0 п.8).

## §4 Тексти (скрипт бере звідси; `@@ <правило> <trig|det>` — роздільник, рядок тексту — наступний)

@@ 1.2 det
**Детектор (К2).** Останній рядок відповіді не збігається з `[█▓░]{12} \d+% · ` → ✗. Маркер ≥85% без `present_files` файла самері в тій самій відповіді → ✗. Відповідь закінчується віджетом `ask_user_input_v0`, а в `question` немає того самого маркера → ✗. *(`▓` — форма інструкції Project; колізія форми — самері G-F §2 Ф-3.)*
@@ 1.3 det
**Детектор (К2).** У файлі самері немає заголовка `## ` зі словами «стартове повідомлення» (без регістру) або після нього немає code block → ✗. Текст самері в чаті без `present_files` → ✗. Ім'я файла не перевіряється: шаблон `BatchXX` ⟂ практика `<Продукт>_session_summary_<id>_<ТЕГ>` (самері G-F §2 Ф-2).
@@ 1.4 det
<!-- K2:n/a — формат, а не поведінка (форма К1/К2, gov-протокол); наявність стартового повідомлення стереже 1.3. Секції цього правила ⟂ практика §0/§1/§2/§6 — самері G-F §2 Ф-1 -->
@@ 1.5 trig
**Тригер.** Будь-яка зміна коду або канону в сесії.
@@ 1.5 det
**Детектор (К2).** `create_file` / `str_replace` / `--confirm` у ході, де після плану немає «так» користувача → ✗ (транскрипт). Для пушу машинно: PLAN-ID `Lens_claude_github_push.py` не пише без двоходового підтвердження. Device-арбітр: id, що стоїть і в `<Продукт>_CHERGA.md`, і в `*_LOCK` → ✗ (межа `Lens_INDEX` §5 — у LOCK лише з device-вироком). Принцип 2 (HTML/CSS першим) сліду не лишає — вирок на рев'ю плану.
@@ 1.6 trig
**Тригер.** Перший файл, створений або змінений у сесії.
@@ 1.6 det
**Детектор (К2).** Артефакт, названий у звіті ходу, відсутній у `ls /mnt/user-data/outputs` → ✗. Робоча копія (`.html`, скрипт, патч-документ; не самері) без суфікса версії `_v\d+` / `_batch\d+(_\d+)?` → ✗. `rev` в `APP_BUILD` ≠ суфікс імені → ✗.
@@ 1.7 det
**Детектор (К2).** Перший білд нового продукту: `grep -cE` по `viewport-fit=cover` · `apple-mobile-web-app-capable` · `rel=["']?manifest`, і `manifest.json` поруч. Будь-який 0 → ✗.
@@ 1.11 trig
**Тригер.** Правка поля, відступу, ширини чи позиції, що має «збігтися» з іншим елементом.
@@ 1.11 det
**Детектор (К2).** План або коміт такої правки не містить імені селектора-еталона і заміряного числа (`\d+(\.\d+)?px`) → ✗.
@@ 1.12 trig
**Тригер.** Видаю інструмент заміру на device-перевірку.
@@ 1.12 det
**Детектор (К2).** У файлі інструмента числа виводяться лише через `console.log`, без запису в DOM (`textContent` / `innerHTML`) → ✗. Немає копіювання рядка в буфер (`navigator.clipboard` або `execCommand('copy')`) → ✗.
@@ 1.13 trig
**Тригер.** Постає питання «знімати тестові дані з білда чи лишати».
@@ 1.13 det
**Детектор (К2).** Білд з увімкненим сідом без `-SEED` в `APP_BUILD` → ✗. `-SEED`-збірка без робочої пари того самого `rev` → ✗.
@@ 2.1 det
**Детектор (К2).** Твердження про стан коду чи канону без tool-read цього файла в тому самому ході → ✗ (родич wsd `12.1`). Рішення, яке memory називає залоченим, дає 0 входжень грепом по репо → ✗: дім не у файлі.
@@ wsd ver
> **Версія 2.36** (17.09.2026 · Lens governance G-F — `К2-1` група 1): детектор (К2) у `1.2` `1.3` `1.5` `1.6` `1.7` `1.11` `1.12` `1.13` `2.1` · `K2:n/a` у `1.4` · тригер (К1) у `1.5` `1.6` `1.11` `1.12` `1.13`.
>
@@ END

# Фармастор v2 — session summary: STEP1 (оболонка+токени) + HANDOFF
> Дата: 12.07.2026. Мета файлу: передати контекст у свіжий чат. **Джерело правди — `Фармастор_v2_MASTER_LOCK.md` (оновлений цієї сесії).**

---

## ⚠️ ГОЛОВНЕ: фідбек юзера (з цього починати новий чат)
Konst подивився крок-1 на девайсі (скріншоти: home dark+light, нативний пікер, FILL) і **незадоволений — і має рацію щодо підходу:**
1. **Очікував harness-first по-екранно** («працюємо кожен екран»), а отримав фундаментний токен/тема-своп під капотом → на екранах майже нічого не змінилось візуально. Розрив очікувань реальний, не дрібниця.
2. **Емоджі-іконку `🏪` в хедері НЕ прибрано** (не була в скоупі кроку-1, але Konst хоче її прибрати). Ймовірно і `📥`/`🗑`/`📋` теж під питання.
3. **Тема-тумблер «працює дивно»** в Claude/Sonnet-прев'ю — треба дивитись на девайсі (можливо auto-dark media-query б'ється з explicit-toggle, або просто прев'ю-артефакт).
4. **Фон «працює дивно візуально»** — треба глянути на девайсі (нейтрали §6.1 vs старий `#f0f4f3`; хедер досі має legacy teal-tint зі старого компонент-CSS).
5. Загальний висновок Konst: «наче нічого не змінилося», «поки що повний відстій» — **треба перекалібрувати підхід** (більше видимого / per-screen), розібрати детально в новому чаті.

**Не оборонятись — це валідний фідбек про cadence.** Перше рішення нового чату: **як вести порт далі** — тримати нинішню чергу §9 (інфраструктура→компоненти) чи перейти на видимий per-screen harness-cadence.

---

## Що ВЖЕ зроблено цієї сесії

### A. Рішення залочені в MASTER_LOCK (файл оновлено, 164 рядки)
- **§8.1 RESOLVED → Home = весь список** (НЕ «на сьогодні»). Route-прив'язку не робимо (scope creep + колега-тестер).
- **§1 refine → `visits[]` масив знімків per-аптека:** `phs[phId].visits=[{date,vals,tier,transferred}]`; **A1 snapshot-on-copy** (📋 → transferred=true; дубль того ж дня=перезапис; новий день=push); absent≠0 у знімках; міграція v1→v2 без бекфілу історії.
- **§5.1 новий → екран порівняння (динаміка залишків), fast-follow, `?OPEN-design UI`:** вхід «📈 Динаміка» на картці (visits≥2) · обираєш 2 знімки від/до · реюз FILL-акордеона `SKU·дата1·дата2·Δ` + тумблер «тільки зміни» · **кодування руху = колір за дистанцією до норми (НЕ за напрямом)** + видиме сире число · absent→«—» · шапка-вердикт «↑N краще·↓N гірше·=N».

### B. Побудовано: крок 1 порту — оболонка тема-агностична + токени §6
Файл: **`Фармастор_замовлення_v2_step1.html`** (база v1.3, 691 рядок, 460К — xlsx-lib ще всередині).
Зміни (тільки HTML/CSS, логіку/DATA не чіпав):
1. `color-scheme:light dark` + **FOUC-скрипт** (тема до paint, ключ `farmstore_theme`).
2. Старий `:root` (baked зеленаві `--teal/--bg/--accent#e8f5f2`) → повний **§6**: нейтрали §6.1, teal §6.2, геометрія §6.3 (як CSS-змінні, застосування — пізніше).
3. **Dual-selector auto-dark** (§7): `[data-theme="dark"]` + `@media(prefers-color-scheme:dark){:root:not([data-theme="light"])}`.
4. **Legacy-міст:** `--teal→var(--accent)`, `--teal-d→--accent-ink`, `--red→--crit`; 4 старих `var(--accent)` (soft-bg role) → `var(--accent-soft)`. Старі компоненти не ламаються, але й не переретемлені — **тому виглядає майже як було** (це і є причина «нічого не змінилось»).
5. `.toast` → `--toast-bg/--toast-ink` (був hardcoded).
6. **Тема-тумблер ◐** (світло→темно→авто, cycleTheme) — стопгеп у `.hdr-top`.

Валідація пройдена: `node --check` 3/3 script-блоки OK · tag-balance div47/47 button8/8 style/script OK · токени §6.2 присутні.

### C. НЕ чіпалось (наступні кроки §8, по черзі §9)
DATA-назви (з `multibrand_harness_v1`) · norm-resolver §3 · copy-контракт §4 + видалення xlsx-lib (−425К) · стейт v2 + getBrand-фікс р.408.

---

## Файли (в /mnt/user-data/outputs, і в проєкті)
- `Фармастор_v2_MASTER_LOCK.md` — **джерело правди (оновлене)**.
- `Фармастор_замовлення_v2_step1.html` — крок-1 білд (на ревʼю/перегляд).
- База порту (read-only): `/mnt/project/Фармастор_замовлення.html` (v1.3).
- Канон назв+компоненти: `Farmastor_multibrand_harness_v1.html`. Колір-реф: `Farmastor_color_harness_v2.html`. MSL: `Фармастор_замовлення_тт.xlsx`.

## Наступні дії (новий чат)
1. **Спершу — перекалібрувати cadence** (per-screen harness vs нинішня черга). Це рішення Konst.
2. Розібрати крок-1 на девайсі: емоджі-іконка, тема-тумблер «дивно», фон «дивно» — що саме прибрати/полагодити.
3. Далі за §9: norm-resolver §3 (або те, що вирішимо після перекалібрування).

READ-FIRST у новому чаті: `Фармастор_v2_MASTER_LOCK.md` → `Work_Standard.md` → `Lens_iOS_cookbook.md`.
